


/**
 * Start and end index.
 * end index is excluded from the answer.
 * it is used to create the slice of the given array.
 * slice is the copying method.
*/
let arr=['a','c','d','e','f','g'];

console.log(arr.slice(2,-5))  //

console.log(arr);
